import { StateAndDispatcher } from './state-and-dispatcher';

describe('StateAndDispatcher', () => {
  it('should create an instance', () => {
    expect(new StateAndDispatcher()).toBeTruthy();
  });
});
